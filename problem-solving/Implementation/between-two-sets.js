/**
 * The function is expected to return an INTEGER.
 * @param {*} a INTEGER_ARRAY
 * @param {*} b INTEGER_ARRAY
 */
function getTotalX(a, b) {
  return 0;
}

let arr = [2, 4];
let brr = [16, 32, 96];
const total = getTotalX(arr, brr);
console.log(total);
